{
  "name": "hardhat-project",
  "scripts": {
    "compile": "hardhat compile",
    "node": "hardhat node",
    "node:two": "hardhat node --port 8546",
    "test:only": "hardhat test",
    "test": "concurrently npm:node npm:node:two npm:test:only -k --hide node,node:two -p \" \""
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "concurrently": "^8.2.2",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
