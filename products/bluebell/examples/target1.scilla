scilla_version 0

library HelloWorld
contract HelloWorld()

field welcome_msg : Uint64 = Uint64 0

transition setHello (msg : Uint64)
  welcome_msg := msg
end

(*

1. Create field table
2. Implement store
3. Remove extra resolve? Check with scilla checker
4. Implement load

*)