scilla_version 0

library HelloWorld
type Bool = 
  | True
  | False



contract HelloWorld()


field welcome_msg : Uint64 = Uint64 0

transition setHello (msg : Uint64)
  is_owner = False;
  match is_owner with
  | True =>
    y = Uint64 1337; (* Hack to compensate for problem stack accounting *)
    x = builtin print msg
(*  | False =>
    welcome_msg := msg    *)
  end
end
